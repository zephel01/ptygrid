# dependencies / build artifacts
node_modules/
dist/
src-tauri/target/
src-tauri/pty-core-check/target/
src-tauri/mcp-server-check/target/

# local user config (may contain secrets/API keys) — commit mterm.example.yml instead
mterm.yml

# editor / OS / logs
*.log
.DS_Store
.idea/
.vscode/
__pycache__/

# local agent runtime state (session locks, scratch writes)
.claude/

# personal notes
docs/note
docs/inside
docs/research

_OUTPUTS/

# ptygrid session cleanup staging (files the device bridge cannot delete)
_to_delete/

# personal working config for this repo (kept out of version control)
# バックアップ(ptygrid.yml.bak-*)も同じ秘密を持ち回るので必ず一緒に除外する。
# 公開用の設定例は ptygrid.example.yml / example/**/ptygrid.yml 側に置く。
/ptygrid.yml
/ptygrid.yml.bak-*
/ptygrid.yml-*
.aider*

src-tauri/target-basemain/

