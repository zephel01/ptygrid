{"default":{"identifier":"default","description":"Default capability for the main window: core APIs (events, window) needed by the terminal pane, plus clipboard READ for the terminal paste paths (right-click paste / Ctrl+Shift+V). Clipboard write is intentionally NOT granted: the frontend copies via navigator.clipboard.writeText.","local":true,"windows":["main"],"permissions":["core:default","clipboard-manager:allow-read-text"]}}