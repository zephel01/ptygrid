{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core APIs (events, window) needed by the terminal pane.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
